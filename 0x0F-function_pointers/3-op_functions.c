#include "3-calc.h"

int op_add(int a, int b);
int op_sub(int a, int b);
int op_mul(int a, int b);
int op_div(int a, int b);
int op_mod(int a, int b);

/**
 * op_add - This file should contain the 5 following functions (not more):.
 * @a: First number.
 * @b: Second number.
 * Return: Sum of a and b.
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub -This file should contain the 5 following functions (not more):.
 * @a: First number.
 * @b: Second number.
 * Return: Difference of a and b.
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - This file should contain the 5 following functions (not more):.
 * @a: First number.
 * @b: Second number.
 * Return: Product of a and b.
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - This file should contain the 5 following functions (not more):.
 * @a: First number.
 * @b: Second number.
 * Return: Quotient of a and b.
 */

int op_div(int a, int b)

{
	return (a / b);
}

/**
 * op_mod - This file should contain the 5 following functions (not more):.
 * @a: First number.
 * @b: Second number.
 * Return: remainder.
 */

int op_mod(int a, int b)
{
	return (a % b);
}
